import { formatDate, diffDate, addDays } from '@/libs/utils/date'
import { currency, substr } from '@/libs/utils/string'
import { formatThousands } from '@/libs/utils/number'

export default {
  formatDate,
  diffDate,
  addDays,
  formatThousands,
  currency,
  substr
}
